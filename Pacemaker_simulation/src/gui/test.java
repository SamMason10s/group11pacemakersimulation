package gui;

import javax.swing.*;
import java.util.Locale;

public class test {
    public static void main(String arg[])
    {
        Locale.setDefault(Locale.ENGLISH);
        JFrame frame = new heart.MyFrame();
    }
}
